# 개인별 입장 안내 설정

`/입장도움말`로 설정 순서를 먼저 확인하세요. 기존에 저장한 채널·역할 연결은 그대로 유지됩니다.
`/입장검사`는 입력 항목 없이 전체 오류를 한 번에 보여줍니다. 미연결/삭제된 역할과 역할 위치 문제는 구분해서 안내합니다.
`/입장역할연결`은 질문 → 선택지 → 역할 → 순서(선택)의 순서이며, 기존 선택지를 자동완성 목록에서 고를 수 있습니다.
출생연도는 기본적으로 07년생부터 90년생 순서입니다. 표시 순서를 바꾸려면 `순서:1`처럼 지정하세요.

새 기능은 **기본적으로 꺼져 있습니다**. 배포만으로 기존 회원의 역할이나
Discord 채널 권한을 변경하지 않습니다. `/입장시작` 이후 신규 입장부터
비공개 스레드로 안내합니다. 기존 첫 채팅 역할 지급과 환영 메시지는 새 기능이
켜진 서버에서만 중단됩니다.

## 1. Discord 서버 준비

- 부스트 레벨 2 이상인 서버에 일반 텍스트 대기 채널을 만듭니다.
- `@everyone`은 대기 채널의 채널 보기·메시지 기록 보기만 허용하고,
  그 밖의 모든 채널/카테고리는 채널 보기를 거부합니다.
  대기 채널에서 직접 글쓰기·스레드 생성은 허용할 필요가 없습니다.
- 기존 기본 회원 역할에 일반 채널과 소개할 채널 4개의 채널 보기를 명시적으로
  허용합니다. 대기 채널 접근도 유지합니다. 소개 도중 접근을 잃으면 안 됩니다.
- 성별·출생연도·관심사 역할에는 일반 채널을 여는 별도 권한을 주지 않습니다.
  관리 권한이 있는 역할은 선택지에 연결할 수 없습니다.
- 서버장과 봇만 볼 수 있는 기록 채널을 만듭니다. 일반 역할/회원의 보기 허용을
  제거합니다. Discord의 관리자 권한은 채널 제한을 우회할 수 있습니다.
- 봇에게 역할 관리 권한을 주고, 봇 역할을 지급할 모든 역할보다 위에 놓습니다.
  대기 채널에서 채널 보기, 메시지/기록 보기, 메시지 보내기, 비공개 스레드 생성,
  스레드 관리, 스레드에서 메시지 보내기를 허용합니다.
- Developer Portal의 **Server Members Intent**를 켭니다. 기존 경험치 기능에
  필요한 Message Content Intent도 유지합니다.
- Discord 자체 온보딩, 다른 봇의 자동 역할, 회원별 채널 권한이 기본 회원 역할이나
  다른 채널 접근을 먼저 지급하지 않도록 전환 시 정리합니다.

비공개 스레드에는 신입과 서버장을 직접 추가하고, 다른 회원 초대는 제한합니다.
스레드 관리 권한자/관리자도 접근할 수 있습니다. 성별/연도 역할 자체는 Discord
프로필에서 다른 회원에게 보일 수 있습니다. 답변은 본인 신고이며 나이 인증이 아닙니다.

## 2. 기본 설정

아래 명령의 채널/역할 칸에서는 Discord에 **이미 있는 항목을 선택**합니다.
복잡한 역할 이름을 복사하거나 ID를 직접 입력할 필요가 없습니다.

```text
/입장기본설정 대기채널:#입장 기본역할:@기존회원역할 기록채널:#입장기록
/입장현황
```

## 3. 질문 선택지와 기존 역할 연결

성별 `남자`, `여자`, 출생연도 `07년생`~`90년생`이 미연결 상태로 준비되어 있습니다.
각 항목을 기존 역할에 연결해야 켤 수 있습니다. 관심사는 원하는 항목을 등록합니다.

```text
/입장역할연결 질문:성별 선택지:남자 역할:@기존남자역할
/입장역할연결 질문:성별 선택지:여자 역할:@기존여자역할
/입장역할연결 질문:출생연도 선택지:07년생 역할:@✧·····07·····✧
/입장역할연결 질문:관심사 선택지:게임 역할:@기존게임역할
/입장역할연결 질문:관심사 선택지:음악 역할:@기존음악역할
/입장현황
```

07년생 예시의 장식은 설명용입니다. 실제 서버 역할을 선택하면 그 역할의 **ID**가
저장됩니다. 이후 이름이 바뀌어도 같은 역할을 지급합니다. 하나의 역할을 여러
선택지나 기본 역할에 중복 연결할 수 없습니다.

- 같은 질문·같은 이름으로 `/입장역할연결`을 실행하면 연결된 역할을 수정합니다.
- 새 연도는 `/입장역할연결`, 허용하지 않을 연도는 `/입장선택지삭제`로 바꿉니다.
- `그 외 나이`는 자동으로 추가되는 고정 항목입니다. 다시 선택 버튼 없이 서버장
  멘션으로 직접 연락하도록 안내합니다. 확인하면 이 절차가 관리하는 역할만
  회수하고 입장을 제한합니다. 추방하지 않습니다.
- 성별/출생연도는 하나, 관심사는 하나 이상 복수 선택입니다.
- 관심사는 고른 뒤 `관심사 선택 확인` 버튼으로 확정합니다. 전체 답변을 확인하고
  `이 정보로 입장하기`를 눌러야 기본 역할과 일반 채널 접근이 주어집니다.
- 다음 질문과 최종 확인 화면의 `다시 선택` 버튼으로 이전 답변을 수정할 수 있습니다.
  채널 소개 중 수정하면 기본 역할을 회수하고, 다시 최종 확인을 마치면 지급합니다.
- 질문 선택 시 기존 안내 메시지만 갱신합니다. 시작 버튼의 개인 스레드 링크와
  오류 안내를 제외하면 별도의 개인 답장을 보내지 않습니다.
- 질문당 최대 25개이며 출생연도는 고정 항목을 제외하고 최대 24개입니다.
- 설정 변경은 새로 시작하는 신입부터 적용됩니다. 진행 중인 사람은 시작 당시
  설정을 유지합니다. 즉시 변경이 필요하면 아래 재시작을 사용합니다.
- 역할을 삭제했다가 같은 이름으로 만들면 ID가 달라집니다. 다시 연결해야 합니다.

## 4. 주요 채널 4개 소개

```text
/입장채널소개 순서:1 채널:#일상 설명:일상 이야기와 인사를 나누는 곳이에요.
/입장채널소개 순서:2 채널:#게임구인 설명:함께 게임할 사람을 찾아보세요.
/입장채널소개 순서:3 채널:#사진 설명:사진과 추억을 공유해보세요.
/입장채널소개 순서:4 채널:#질문 설명:궁금한 점을 물어보세요.
```

실제 서버의 채널과 원하는 설명을 선택합니다. 기본 역할 지급이 성공한 뒤
채널 링크를 하나씩 소개합니다. 네 번째에서 안내 완료를 누르면 전체 링크를
남기고 스레드를 잠금·보관합니다.

## 5. 검사, 테스트, 켜기

```text
/입장검사
/입장안내게시
/입장시작
```

권한이나 연결이 빠졌으면 검사 결과에 나온 항목부터 수정합니다. 켜기 전에
안내 버튼을 게시해두면 신규 회원이 봇 재시작/일시 장애 후 이어갈 수 있습니다.
봇이 오프라인일 때 들어온 회원은 대기 채널의 시작/이어하기 버튼을 누르면 됩니다.

기본 역할이 없는 테스트 계정으로 다음을 확인한 후 기존 Discord 온보딩의 역할
지급을 중단하고 전환합니다. 이 저장소에는 배포 설정이 없으므로 호스팅 서비스의
배포 브랜치/자동 배포 여부는 서비스 대시보드에서 별도로 확인해야 합니다.

1. 새 입장 시 개인 비공개 스레드가 생성되고 다른 신입은 읽을 수 없는지
2. 07년생 선택 시 기존 장식 역할이 정확히 지급되는지
3. 관심사를 여러 개 고른 뒤 확인해야 역할이 지급되고, 최종 입장 확인 후 일반 채널이 열리는지
4. 그 외 나이에서 다시 선택 없이 서버장 멘션이 표시되고 확정 후 역할이 회수되는지
5. 두 사람이 동시에 진행하거나 버튼을 연속으로 눌러도 섞이지 않는지
6. 질문 중 봇을 재시작한 후 기존 버튼 또는 이어하기로 계속할 수 있는지
7. 역할 위치를 잘못 설정했을 때 기본 역할이 먼저 지급되지 않는지
8. 소개 4개와 완료 후 스레드 잠금·보관이 동작하는지
9. 다시 선택으로 성별·출생연도·관심사를 바꿀 수 있고 이전 역할이 교체되는지
10. 탈퇴 후 재입장하면 기존 스레드가 다시 열리고 처음부터 질문하는지

## 운영 및 복구

```text
/입장이어하기 대상:@신입
/입장재시작 대상:@신입
/입장중지
```

이어하기는 저장된 절차를 재개합니다. 재시작은 해당 회원의 이전 입장 설정에
속한 역할(기본 역할 포함)을 회수하고 이전 스레드를 보관한 뒤 현재 설정으로
다시 시작합니다. 다른 역할은 건드리지 않습니다. 거절 선택 실수도 재시작으로
복구할 수 있습니다. **끄기는 기존 첫 채팅 역할 지급 방식을 다시 활성화**하므로
운영 중단용 전체 차단 기능으로 사용하지 마세요.

Firebase의 `onboarding/config/{서버ID}`와
`onboarding/sessions/{서버ID}/{회원ID}`에 설정/진행/답변/스레드 ID를 저장합니다.
다른 서버와 분리되며 기존 경험치는 보존합니다. 별도의 DB 규칙 변경은 하지 않습니다.
현재 보관 정책은 세션 유지이며 재시작/재입장 시 해당 회원의 세션을 교체합니다.
스레드와 기록 채널의 과거 메시지는 자동 삭제하지 않습니다.

재입장 시에는 기존 스레드를 다시 열어 이전 방문을 환영하는 안내와 새 질문을
보냅니다. 이전 답변은 재사용하지 않습니다. 기존 스레드가 삭제되었다면 새로 만듭니다.
스레드 이름은 `입장 : 서버닉네임/YYYY-MM-DD` 형식이며 날짜는 한국 시간의
서버 입장일입니다. Discord의 이름 길이 제한에 맞게 긴 닉네임은 줄입니다.

역할 지급은 외부 API이므로 DB와 단일 트랜잭션으로 묶을 수 없습니다. 지급 전
진행 의도를 저장하고 재시도 시 실제 역할 상태를 다시 읽어 중복 지급 없이 복구합니다.
기본 역할은 질문 역할과 사용자 데이터 준비가 성공한 뒤 마지막에 지급합니다.
봇은 한 인스턴스로 실행하세요. 중복 클릭 잠금은 프로세스 내부 잠금입니다.

## 개발 검증

실제 Discord 로그인/Firebase 쓰기 없이 테스트합니다.

```text
python -m unittest discover -s tests -v
python -m py_compile main.py onboarding.py
```

실제 서버 권한/알림 도착과 배포 동작은 위 수동 테스트가 필요합니다.
